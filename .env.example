# Google Maps API key — keep this private. Never commit the real .env file.
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Bootstrap admin account (created automatically on first launch if not already present)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change_me_now
